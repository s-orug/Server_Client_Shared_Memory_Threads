#include "text-memory.h"

int main() {
  /*char *k = attach_segment(create_segment(create_key("dat/dante.txt")));
  strncpy(k, "works\n", BLOCK_SIZE);
  detach_segment(k);*/
  char k;
//  scanf("%c",&k);
  printf("%c\n", 61);//==EOT?"true":"false");
  return 0;
}